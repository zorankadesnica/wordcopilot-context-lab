# Data

This repository uses a real public dataset at runtime instead of checking large files into Git.

Default dataset: **QMSum** via Hugging Face `datasets`.

The scripts flatten each long meeting transcript into `(document, query, reference answer)` examples.
